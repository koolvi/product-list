import React from 'react';
import {
  TouchableOpacity,
} from 'react-native';


const Checkbox = (props) => {
  const { icon } = props;

  return (
    <TouchableOpacity
      onPress={() => {}}
    >
      {icon}
    </TouchableOpacity>
  );
};

export default Checkbox;
